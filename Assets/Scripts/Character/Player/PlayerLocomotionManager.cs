using UnityEngine;

namespace SG {
    public class PlayerLocomotionManager : MonoBehaviour
    {
        public void HandleAllMovement()
        {
            // Перемещение по земле (наземное движение)
            // Перемещение в воздухе (воздушное движение)
        }
    }
}